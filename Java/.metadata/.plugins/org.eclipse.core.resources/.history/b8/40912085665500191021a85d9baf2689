package main;

import renderers.Window;
import renderers.Menu;

public class Main {

	public static void main(String[] args) {
		Window window = new Window(800, 600);
		Menu menu = new Menu(window.getFrame(), null, null);
		menu.show();
		window.show();

	}

}
